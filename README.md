# 🏎️ AI-Racer: Автономные гонки с обучением через Reinforcement Learning

Проект представляет собой симулятор гоночной трассы с искусственным интеллектом, который обучается управлять машиной с помощью алгоритма PPO (Proximal Policy Optimization) из библиотеки Stable-Baselines3.

## 📋 Описание

AI-Racer - это среда для обучения нейронной сети управлению гоночной машиной на сложной трассе. Машина использует систему лидаров (7 лучей) для обнаружения препятствий и должна проходить чекпоинты, избегая столкновений со стенами и движения в неправильном направлении.

### Основные возможности

- 🎮 **Интерактивная визуализация** - наблюдение за обучением в реальном времени
- 🧠 **Обучение через PPO** - использование современного алгоритма reinforcement learning
- 🏁 **Сложная трасса** - трасса с плавными поворотами и закругленными углами
- ⚡ **Система наград** - награды за прохождение чекпоинтов, штрафы за ошибки
- 🔄 **Автоматический сброс** - машина возвращается на старт при движении в неправильном направлении

## 🚀 Установка

### Требования

- Python 3.8 или выше
- pip

### Шаги установки

1. Клонируйте репозиторий или скачайте проект:
```bash
git clone <repository-url>
cd AI-Racer
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Зависимости

Проект использует следующие библиотеки:
- `gymnasium==1.0.0` - среда для reinforcement learning
- `pygame==2.5.2` - графическая библиотека для визуализации
- `stable-baselines3==2.3.0` - алгоритмы RL (PPO)
- `numpy==1.24.0` - численные вычисления
- `torch==2.3.0` - PyTorch для нейронных сетей
- `shimmy==1.3.0` - совместимость с Gymnasium
- `opencv-python==4.9.0.80` - обработка изображений

## 🎮 Использование

### Запуск обучения

Просто запустите основной файл:

```bash
python play_race.py
```

### Как это работает

1. **Фаза обучения**: AI обучается на 10000 шагов без графики (быстро)
2. **Фаза демонстрации**: Показывается 2 заезда с визуализацией
3. **Повтор**: Процесс повторяется бесконечно до остановки (Ctrl+C)

### Продолжение обучения

Чтобы продолжить обучение с сохраненной модели, раскомментируйте строку в `play_race.py`:

```python
# model = PPO.load("models/CyberLive/650000", env=env_train)
```

И закомментируйте создание новой модели:

```python
# model = PPO("MlpPolicy", env_train, verbose=0, tensorboard_log=LOG_DIR, device="auto")
```

## 📁 Структура проекта

```
AI-Racer/
├── race_env.py          # Среда Gymnasium с трассой и физикой
├── play_race.py         # Основной файл для обучения и демонстрации
├── requirements.txt     # Зависимости проекта
├── README.md           # Этот файл
├── models/              # Сохраненные модели (создается автоматически)
│   └── CyberLive/      # Модели по поколениям
└── logs_cyber_live/     # Логи TensorBoard (создается автоматически)
```

## 🎯 Механика игры

### Система наблюдений

Машина получает 9 значений:
- **7 лучей лидара** - расстояние до препятствий в разных направлениях
- **Скорость** - нормализованная скорость машины (0-1)
- **Угол поворота** - синус угла направления машины

### Действия

Машина может выполнять 2 действия:
- **Руль** (-1 до 1): Поворот влево/вправо
- **Газ** (-1 до 1): Ускорение/торможение

### Система наград

- ✅ **+20** - прохождение чекпоинта
- ✅ **+1000** - завершение круга
- ❌ **-50** - столкновение со стеной
- ❌ **-100** - движение в неправильном направлении (разворот)
- ❌ **-0.05** - штраф за каждый шаг (стимулирует быстрое движение)

### Правила

- Машина должна проходить чекпоинты по порядку
- При столкновении со стеной эпизод завершается
- При движении назад или в неправильном направлении машина сбрасывается на старт
- Максимальная скорость: 15 единиц
- Минимальная скорость (задний ход): -5 единиц

## 🎨 Визуальные особенности

- **Трасса**: Темно-серая трасса с плавными закругленными углами (Catmull-Rom сплайны)
- **Контур**: Белый контур вокруг трассы
- **Разметка**: Одна белая полоска по центру трассы
- **Машинка**: Красная машина с белым "окном" спереди
- **Лидары**: Зеленые лучи для обнаружения препятствий (красные при близком расстоянии)
- **Чекпоинты**: Зеленые прямоугольники показывают следующий чекпоинт

## ⚙️ Настройки

В файле `play_race.py` можно изменить:

```python
SHOW_EVERY_STEPS = 10000  # Количество шагов обучения между демонстрациями
EPISODES_TO_SHOW = 2      # Количество заездов для показа
```

## 📊 Мониторинг обучения

Логи TensorBoard сохраняются в папке `logs_cyber_live/`. Для просмотра:

```bash
tensorboard --logdir=logs_cyber_live
```

## 🛠️ Разработка

### Архитектура

- **CyberRacingEnv**: Класс среды, наследуется от `gym.Env`
  - `reset()`: Сброс среды в начальное состояние
  - `step()`: Выполнение одного шага симуляции
  - `_get_obs()`: Получение наблюдений (лидары + скорость + угол)
  - `_render_frame()`: Отрисовка кадра
  - `_smooth_track_points()`: Сглаживание углов трассы

### Физика

- Угол поворота изменяется на `steering * 0.15` радиан
- Скорость изменяется на `throttle * 0.5`
- Трение: скорость умножается на `0.95` каждый шаг
- Максимальная скорость: 15, минимальная (задний ход): -5

## 🐛 Решение проблем

### Проблема: pygame не инициализируется
**Решение**: Убедитесь, что установлена правильная версия pygame: `pip install pygame==2.5.2`

### Проблема: Модель не обучается
**Решение**: Проверьте, что все зависимости установлены и версии соответствуют `requirements.txt`

### Проблема: Окно не закрывается
**Решение**: Нажмите Ctrl+C в терминале или закройте окно через крестик

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Проект разработан для изучения reinforcement learning и создания автономных агентов.

## 🙏 Благодарности

- Stable-Baselines3 за отличную библиотеку RL алгоритмов
- Gymnasium за стандартизированный интерфейс сред
- Pygame за простую и эффективную графику

---

**Приятного обучения! 🏁**

